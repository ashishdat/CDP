# Phase 7A.12 Family × Source Matrix

Status: **EMPTY — NO QUALIFIED ASSETS**.

The intake coverage matrix records qualified counts for CMS, UB, custom claims, support, non-claim, and unknown by source. The LOSO matrix adds taxonomy accuracy, processing-route accuracy, false-standard authorizations, safe fallbacks, and mean routing risk for every populated cell. Missing family/source cells remain visible as coverage gaps and are never treated as zero-error observations.
