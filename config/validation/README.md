# config/validation

Configurable field-level validation thresholds (critical vs non-critical),
reference data for ICD-10/CPT/HCPCS adapters, and modifier tables consumed
by `packages/validation_rules` (Phase 3).
