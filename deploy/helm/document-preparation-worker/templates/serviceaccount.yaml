{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "document-preparation-worker.serviceAccountName" . }}
  labels:
    {{- include "document-preparation-worker.labels" . | nindent 4 }}
{{- end }}
