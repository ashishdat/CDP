apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "human-review-api.fullname" . }}-config
  labels:
    {{- include "human-review-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
