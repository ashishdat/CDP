apiVersion: v1
kind: Service
metadata:
  name: {{ include "output-api.fullname" . }}
  labels:
    {{- include "output-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "output-api.selectorLabels" . | nindent 4 }}
