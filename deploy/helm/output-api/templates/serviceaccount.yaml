{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "output-api.serviceAccountName" . }}
  labels:
    {{- include "output-api.labels" . | nindent 4 }}
{{- end }}
