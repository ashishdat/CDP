# tests/golden

Byte-for-byte comparisons of generated fixed-width output against the
supplied reference `.txt` files (Phase 3+). `fixtures/` holds hand-built
synthetic claim documents/expected-output pairs — never copies of the
supplied dataset (see docs/DATASET_FINDINGS.md for why).
