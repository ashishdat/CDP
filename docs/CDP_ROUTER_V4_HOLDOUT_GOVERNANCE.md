# Router V4 independent holdout governance

An external holdout requires independently owned/acquired documents, complete provenance, authorization, document and truth hashes, development non-use attestation, and `FROZEN_UNTOUCHED` status before routing. The gate refuses access without a frozen Router V4 candidate. Generated evidence must be described honestly as engineering evidence, never historical production data.

