# Visual Router Margin Analysis

The two known false-standard predictions were separable from typical correct UB predictions by uncertainty, but margin is not sufficient as a safety policy. A low margin can identify ambiguity; it cannot establish the correct family. VC-05 therefore permits uncertainty only as an abstention signal and never as positive standard-route evidence.
