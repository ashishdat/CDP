# config/templates

Versioned CMS-1500 / UB-04 template definitions consumed by
`packages/templates` (Phase 2): anchor phrases, alignment points, field and
service-line regions, per `template_id`/`version`.
