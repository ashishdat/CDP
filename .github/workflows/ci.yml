name: quality

on:
  push:
    branches: [main, phase8-20-claim-bundle-optimization, fix/strict-claim-form-identity-gate]
  pull_request:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - run: git diff --check
      - run: python scripts/check_architecture.py
      - run: ruff check evaluation/strict_identity_cached_replay.py evaluation/real_archive_classification.py packages/document_routing/router.py packages/document_routing/decision_service.py packages/document_routing/evidence.py packages/processing_routes/resolver.py packages/standard_form_verification/__init__.py packages/standard_form_verification/cms1500.py packages/standard_form_verification/contracts.py packages/standard_form_verification/evidence.py packages/standard_form_verification/ub04.py tests/unit/cases/test_strict_identity_adversarial_chain.py tests/unit/cases/test_strict_identity_cached_replay.py
      - run: mypy --follow-imports=skip --ignore-missing-imports evaluation/strict_identity_cached_replay.py evaluation/real_archive_classification.py packages/document_routing/router.py packages/document_routing/decision_service.py packages/document_routing/evidence.py packages/processing_routes/resolver.py packages/standard_form_verification
      - run: ruff check packages/runtime_profile packages/evidence_decision packages/production_readiness_gate.py evaluation/phase8_12_tesseract_confirmation.py evaluation/phase8_12_reproducibility.py
      - run: pytest tests/unit tests/architecture -q -p no:cacheprovider --basetemp=.test-tmp/pytest

  integration:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - run: pytest tests/integration -q -p no:cacheprovider --basetemp=.test-tmp/integration

  governed-replay:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - run: python -m evaluation.phase8_12_reproducibility replay | tee phase8_12_replay_report.json
      - uses: actions/upload-artifact@v4
        with:
          name: phase8-12-governed-replay
          path: |
            phase8_12_replay_report.json
            evaluation/baselines/phase8_12/manifest.json

  policy-governance:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - run: pytest tests/unit/cases/test_production_readiness_gate.py tests/unit/cases/test_route_registry.py tests/integration/test_phase8_10b_runtime_profile_parity.py -q -p no:cacheprovider --basetemp=.test-tmp/governance

  evaluation-ui:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: apps/evaluation_ui
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/evaluation_ui/package-lock.json
      - run: npm ci
      - run: npm audit --audit-level=high
      - run: npm test -- --run
      - run: npm run build
