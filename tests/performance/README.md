# tests/performance

Throughput/latency harness (pages/sec, p50/p95/p99, OCR/VLM invocation
rates, estimated cost per page, straight-through rate). Phase 5. Run via
`pytest -m performance` — not part of `make test`.
