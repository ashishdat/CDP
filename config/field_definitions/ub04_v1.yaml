version: ub04-field-graph-v1
fields:
  - {field_name: member_id, form_family: UB04, aliases: ["INSURED'S UNIQUE ID", "MEMBER ID", "INSURED ID"], page_zone: MIDDLE, relationships: [{relation: right_of, x0_offset: 0.02, y0_offset: -0.01, x1_offset: 0.30, y1_offset: 0.04}], neighbor_fields: [patient_name], negative_labels: [], datatype: ALPHANUMERIC_ID, blocking: true, criticality: CRITICAL, secondary_ocr_policy: RAPID_THEN_TESSERACT, validation_policy: MEMBER_ID}
  - {field_name: patient_name, form_family: UB04, aliases: ["PATIENT NAME", "PATIENT'S NAME"], page_zone: UPPER_LEFT, relationships: [{relation: below, x0_offset: 0.00, y0_offset: 0.015, x1_offset: 0.42, y1_offset: 0.065}], neighbor_fields: [patient_dob], negative_labels: [], datatype: PERSON_NAME, blocking: true, criticality: CRITICAL, secondary_ocr_policy: RAPID_THEN_PADDLE, validation_policy: PERSON_NAME}
  - {field_name: patient_dob, form_family: UB04, aliases: ["BIRTHDATE", "DATE OF BIRTH", DOB], page_zone: UPPER_MIDDLE, relationships: [{relation: below, x0_offset: 0.00, y0_offset: 0.015, x1_offset: 0.20, y1_offset: 0.06}], neighbor_fields: [patient_name], negative_labels: [], datatype: DATE, blocking: true, criticality: CRITICAL, secondary_ocr_policy: RAPID_THEN_TESSERACT_DATE, validation_policy: DATE}
  - {field_name: provider_name, form_family: UB04, aliases: ["BILLING PROVIDER", "PROVIDER NAME", "FACILITY / PROVIDER", "FACILITY PROVIDER"], page_zone: UPPER_LEFT, relationships: [{relation: below, x0_offset: 0.00, y0_offset: 0.015, x1_offset: 0.45, y1_offset: 0.075}], neighbor_fields: [provider_npi], negative_labels: [], datatype: PERSON_OR_ORGANIZATION, secondary_ocr_policy: RAPID_THEN_PADDLE, validation_policy: NON_EMPTY}
  - {field_name: provider_npi, form_family: UB04, aliases: [NPI, "PROVIDER NPI"], page_zone: UPPER_RIGHT, relationships: [{relation: right_of, x0_offset: 0.04, y0_offset: -0.01, x1_offset: 0.24, y1_offset: 0.035}], neighbor_fields: [provider_name], negative_labels: [], datatype: NPI, blocking: true, criticality: CRITICAL, secondary_ocr_policy: RAPID_THEN_TESSERACT, validation_policy: NPI_LUHN}
  - {field_name: federal_tax_no, form_family: UB04, aliases: ["FEDERAL TAX NO", "FEDERAL TAX NUMBER", "FEDERAL TAX ID", "TAX ID", EIN], page_zone: UPPER_RIGHT, relationships: [{relation: below, x0_offset: 0.00, y0_offset: 0.012, x1_offset: 0.20, y1_offset: 0.055}], neighbor_fields: [provider_npi, type_of_bill], negative_labels: [NPI, "TYPE OF BILL"], datatype: TAX_IDENTIFIER, blocking: true, criticality: CRITICAL, secondary_ocr_policy: RAPID_THEN_TESSERACT, validation_policy: TAX_IDENTIFIER_SYNTAX}
  - {field_name: type_of_bill, form_family: UB04, aliases: ["TYPE OF BILL", TOB], page_zone: UPPER_RIGHT, relationships: [{relation: right_of, x0_offset: 0.03, y0_offset: -0.01, x1_offset: 0.15, y1_offset: 0.04}], neighbor_fields: [], negative_labels: [], datatype: TYPE_OF_BILL, blocking: true, criticality: CRITICAL, secondary_ocr_policy: RAPID_THEN_TESSERACT, validation_policy: TYPE_OF_BILL}
  - {field_name: principal_diagnosis, form_family: UB04, aliases: ["PRINCIPAL DIAGNOSIS", "PRIN DIAG"], page_zone: LOWER_LEFT, relationships: [{relation: right_of, x0_offset: 0.02, y0_offset: -0.01, x1_offset: 0.25, y1_offset: 0.04}], neighbor_fields: [], negative_labels: [], datatype: ICD_CODE, blocking: true, criticality: CRITICAL, secondary_ocr_policy: RAPID_THEN_TESSERACT, validation_policy: ICD}
  - {field_name: total_charge, form_family: UB04, aliases: ["TOTAL CHARGES", "TOTAL CHARGE"], page_zone: LOWER_RIGHT, relationships: [{relation: right_of, x0_offset: 0.03, y0_offset: -0.01, x1_offset: 0.22, y1_offset: 0.04}], neighbor_fields: [], negative_labels: [NON COVERED], datatype: CURRENCY, blocking: true, criticality: CRITICAL, secondary_ocr_policy: RAPID_THEN_TESSERACT, validation_policy: CURRENCY}
