# Visual evidence latency

Cold evaluation P95 was 51.65 ms A→B and 57.42 ms B→A. Warm comparison P95 was 41.70–44.82 ms. The model is 34–35 KB, CPU-only, requires no GPU and has zero cloud cost. Conservative promotion uses the cold result, which is slightly above the preferred 50 ms but below the 100 ms total-increase preference.

