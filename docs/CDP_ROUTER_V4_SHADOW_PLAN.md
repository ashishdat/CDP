# Router V4 shadow plan

Shadow is blocked until cross-source and one-shot independent-holdout gates pass. When eligible, shadow may record decisions, evidence, latency, CPU, OCR calls, agreement, and later reviewer truth. It must not alter the canonical route, extractor, output, HITL, or STP. False standard routes remain a fail-closed safety gate.
