# ML router adversarial safety

The adversarial partitions contain custom structured and non-claim pages with healthcare terminology. Deterministically corroborated LightGBM fusion introduced zero CMS false eligibility, zero UB false eligibility, zero dual-standard eligibility and zero final false-standard routes in both cross-source directions.

Safety passed, but coverage did not. Missing `UNKNOWN_UNSTRUCTURED` examples and small adversarial samples prevent a promotion claim.

