# config/schemas

JSON Schema / Pydantic-exported schemas for the canonical claim JSON, keyed
by `schema_version` (see `packages.domain.claim.Claim`). Populated in
Phase 3 alongside `packages/fixed_width`.
