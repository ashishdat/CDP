apiVersion: v1
kind: Service
metadata:
  name: {{ include "human-review-api.fullname" . }}
  labels:
    {{- include "human-review-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "human-review-api.selectorLabels" . | nindent 4 }}
