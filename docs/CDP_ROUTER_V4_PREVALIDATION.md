# Router V4 prevalidation baseline

`ROUTER_V4_PREVALIDATION_BASELINE` was frozen before the cross-source run. The machine-readable manifest is under `evaluation_results/router_v4/prevalidation/manifest.json` and records commit `2c9ab189`, all implementation/configuration hashes, routing-input v4.0, normalized-structure v4.0, decision contract 4.0, Tesseract 5.4.0, and disabled V3/V4 runtime flags. The tracked worktree was dirty, so the file hashes—not the commit alone—are the authoritative baseline identity.

